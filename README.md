# bootstrap

This repository contains a [bootstrap.sh](./bootstrap.sh) script for installing common tools on a new Ubuntu machine.

To run:

```sh
curl -s https://raw.githubusercontent.com/cecheta/bootstrap/refs/heads/master/bootstrap.sh | bash
```
